services:
  web:
    build: .
    ports:
      - "8080:8080"
  postgres:
    image: postgres:latest
    container_name: my_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: psw
      POSTGRES_DB: db
    ports:
      - "5432:5432"
